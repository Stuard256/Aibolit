# 📁 Структура проекта Aibolit

Полное описание организации файлов и папок проекта.

---

## 🗂️ Основные папки

### 📚 **docs/** - Документация
Вся документация проекта собрана в одном месте.

```
docs/
├── README.md                    # Навигация по документации
├── УСТАНОВКА.md                # Руководство по установке и запуску
├── ИЗМЕНЕНИЯ.md                # История всех изменений
├── WKHTMLTOPDF.md              # Установка PDF генератора
├── СТРУКТУРА_ПРОЕКТА.md        # Этот файл
└── PROJECT_DOCUMENTATION.md    # Техническая документация
```

**Назначение:** Централизованное хранение всей документации

---

### 📊 **data/** - Данные для импорта
CSV файлы с тестовыми и реальными данными.

```
data/
├── test_.csv                   # Основной тестовый набор данных
├── animals_temp.csv           # Временные данные о животных
├── owners_test.csv            # Тестовые владельцы
├── vac_for_test2.csv          # Данные о вакцинациях
└── advanced_veterinary_dataset.csv  # Расширенный датасет
```

**Назначение:** Хранение CSV файлов для импорта в базу данных

**Использование:**
```bash
python3 -m flask import-csv      # Импортирует data/test_.csv
python3 -m flask import-vac-csv  # Импортирует data/vac_for_test2.csv
```

---

### 🤖 **ml/** - Машинное обучение
Всё, что связано с ML диагностикой.

```
ml/
├── models/                            # Обученные модели
│   ├── weighted_animal_disease_model.pkl
│   ├── animal_disease_model.pkl
│   └── focused_dog_disease_model.pkl
│
├── datasets/                          # Генераторы датасетов
│   ├── dog_disease_dataset.py        # Датасет для собак
│   └── cat_disease_dataset.py        # Датасет для кошек
│
├── docs/                              # ML документация
│   ├── ML_STATUS.md                  # Текущий статус ML системы
│   ├── DEBUG_ML.md                   # Отладочная информация
│   ├── requirements_ml.txt           # Зависимости для ML
│   ├── ML_DIAGNOSIS_README.md
│   ├── EXTENDED_DIAGNOSIS_README.md
│   └── SETUP_ML.md
│
├── ml_model.py                        # Классы ML моделей
├── train_model.py                     # Обучение основной модели
├── train_focused_model.py             # Обучение специализированных моделей
├── real_veterinary_dataset.py         # Генератор реальных данных
└── advanced_veterinary_dataset.py     # Расширенный генератор
```

**Назначение:** Организация ML компонентов системы

**Важно:** 
- Модели загружаются из `ml/models/` 
- При создании новых моделей сохраняйте их в `ml/models/`

---

### 🧪 **tests/** - Тестовые файлы
Все тесты и проверочные скрипты.

```
tests/
├── test_api.py                # Тесты API
├── test_ml.py                 # Тесты ML моделей
├── test_csrf.py               # Тесты CSRF защиты
├── test_new_model.py          # Тесты новой модели
├── test_rabies.py             # Тесты отчётов о бешенстве
├── check_model.py             # Проверка модели
├── check_server.py            # Проверка сервера
├── check_rabies_data.py       # Проверка данных о бешенстве
├── simple_test.py             # Простые тесты
└── run_test.py                # Запуск тестов
```

**Назначение:** Изоляция тестовых файлов от основного кода

---

### 🎨 **templates/** - HTML шаблоны
Все HTML шаблоны приложения.

```
templates/
├── base.html                  # Базовый шаблон с навигацией
├── index.html                 # Главная страница
├── settings.html              # Настройки
├── statistics.html            # Статистика
│
├── owner_card.html            # Карточка владельца
├── owners.html                # Список владельцев
├── add_owner.html             # Добавление владельца
│
├── pet_card.html              # Карточка животного
│
├── appointment_details.html   # Детали приёма
├── appointment_form.html      # Форма приёма
│
├── vaccination_form.html      # Форма вакцинации
├── vaccinations.html          # Отчёты по вакцинациям
├── vaccinations_list.html     # Список вакцинаций
├── rabies_report.html         # Отчёт о бешенстве
│
├── treatment_calculator.html  # Калькулятор назначений
├── treatments.html            # Список назначений
├── add_treatment.html         # Добавление назначения
├── edit_treatment.html        # Редактирование назначения
│
├── diagnosis.html             # Диагностика
├── diagnosis_extended.html    # Расширенная диагностика
│
├── available_cards.html       # Свободные номера карточек
├── admin.html                 # Админ панель
└── ...
```

---

### 🎨 **static/** - Статические файлы
CSS, JavaScript, изображения.

```
static/
└── css/
    └── styles.css             # Основные стили
```

---

### 💾 **instance/** - База данных
Файл базы данных SQLite (создаётся автоматически).

```
instance/
└── vet_clinic.db             # База данных
```

**Примечание:** Эта папка создаётся Flask автоматически

---

### 💾 **database_backups/** - Резервные копии
Автоматические бэкапы базы данных.

```
database_backups/
├── vet_clinic_backup_20251106_120000.db
├── vet_clinic_backup_20251106_121500.db
└── ... (хранятся последние 10)
```

**Автоматизация:** 
- Бэкапы создаются каждые 15 минут
- Хранятся последние 10 копий

---

## 📄 Основные файлы в корне

### Python файлы
```
app.py                    # Главное Flask приложение
models.py                 # Модели БД (SQLAlchemy)
forms.py                  # Формы (WTForms)
csv_importer.py          # Импорт данных из CSV
phone_normalizer.py      # Нормализация телефонов
```

### Конфигурационные файлы
```
requirements.txt         # Python зависимости
.gitignore              # Git ignore
package.json            # Node.js зависимости (если есть)
```

### Шаблоны документов
```
card.docx               # Шаблон карточки животного для печати
```

### BAT скрипты (Windows)
```
aibolit.bat             # Простой запуск
aibolit_safe.bat        # Запуск с проверками
reset_db.bat            # Сброс БД
load_cards_in_db.bat    # Импорт карточек
load_vaccines_in_db.bat # Импорт вакцинаций
normalize_phones.bat    # Нормализация телефонов
update.bat              # Обновление из Git
setup.bat               # Первоначальная настройка
```

---

## 🔄 Импорты и зависимости

### Пути к ML моделям
```python
# В app.py модели загружаются из:
'ml/models/weighted_animal_disease_model.pkl'
'ml/models/improved_animal_disease_model.pkl'
'ml/models/animal_disease_model.pkl'
```

### Пути к CSV файлам
```python
# В app.py команды импорта используют:
'data/test_.csv'
'data/animals_temp.csv'
'data/owners_test.csv'
'data/vac_for_test2.csv'
```

### Импорты между модулями
```python
# app.py импортирует:
from models import db, Owner, Pet, Appointment, ...
from forms import TreatmentForm, TreatmentCalculatorForm
from csv_importer import import_csv, import_vaccinations, ...
```

---

## 📊 Размер проекта

### Статистика файлов
- **Python файлы:** ~20
- **HTML шаблоны:** ~20
- **Тестовые файлы:** ~10
- **Документация:** ~10 MD файлов
- **CSV данные:** ~5 файлов
- **ML модели:** ~3 файла
- **BAT скрипты:** ~8 файлов

### Строк кода (приблизительно)
- **app.py:** ~2400 строк
- **models.py:** ~200 строк
- **Шаблоны:** ~4000 строк HTML/Jinja2
- **Всего Python:** ~3000+ строк

---

## 🎯 Преимущества новой структуры

### ✅ Организация
- Каждый тип файлов в своей папке
- Легко найти нужный файл
- Чистый корень проекта

### ✅ Масштабируемость
- Легко добавлять новые тесты в `tests/`
- Легко добавлять новую документацию в `docs/`
- Легко добавлять новые датасеты в `data/`

### ✅ Поддержка
- Вся документация в одном месте
- Понятная структура для новых разработчиков
- Соответствует best practices

---

## 🔍 Навигация по проекту

### Хочу найти...

**...документацию** → `docs/`

**...тесты** → `tests/`

**...ML код** → `ml/`

**...данные для импорта** → `data/`

**...HTML шаблоны** → `templates/`

**...основной код** → корень проекта (`app.py`, `models.py`, `forms.py`)

**...скрипты запуска** → корень проекта (`*.bat`)

---

## 📞 Дополнительная информация

См. также:
- [docs/README.md](README.md) - Навигация по документации
- [docs/УСТАНОВКА.md](УСТАНОВКА.md) - Установка и запуск
- [README.md](../README.md) - Главный README проекта

---

**Структура актуальна на: 6 ноября 2025**

