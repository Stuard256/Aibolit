# 🚀 Установка и запуск Aibolit

Полное руководство по установке и запуску системы управления ветеринарной клиникой.

---

## 📋 Требования

### Системные требования:
- **Операционная система:** Windows 10/11
- **Python:** 3.10 или выше (3.6+ поддерживается)
- **Память:** 2 GB RAM (рекомендуется 4 GB)
- **Место на диске:** 500 MB

### ВАЖНО! Версия Python

На вашем компьютере могут быть установлены ДВЕ версии Python:
- ❌ **Python 2.7** (команда `python`) - НЕ ПОДДЕРЖИВАЕТСЯ
- ✅ **Python 3.10+** (команда `python3`) - ИСПОЛЬЗУЙТЕ ЭТУ

Программа работает **ТОЛЬКО** с Python 3!

---

## ⚡ Быстрый старт

### Способ 1: Автоматический запуск (рекомендуется)

Дважды кликните на один из файлов:
- **`aibolit.bat`** - простой запуск
- **`aibolit_safe.bat`** - запуск с проверкой зависимостей

Программа откроется в браузере автоматически!

### Способ 2: Ручной запуск

Откройте командную строку и введите:

```bash
python3 app.py
```

Затем откройте браузер: **http://localhost:5000**

---

## 📦 Установка зависимостей

Если программа не запускается или просит установить модули:

### 1. Основные зависимости

```bash
python3 -m pip install -r requirements.txt
```

### 2. ML библиотеки (для диагностики)

```bash
python3 -m pip install joblib scikit-learn numpy pandas
```

### 3. Проверка установки

Запустите `aibolit_safe.bat` - он автоматически проверит и установит недостающие пакеты.

---

## 🔧 Дополнительные команды

### Работа с базой данных

```bash
reset_db.bat              # Сброс базы данных
load_cards_in_db.bat      # Импорт карточек из CSV
load_vaccines_in_db.bat   # Импорт вакцинаций из CSV
normalize_phones.bat      # Нормализация телефонных номеров
```

### Обновление программы

```bash
update.bat                # Обновление из Git репозитория
```

---

## 🎨 Основные функции

После запуска программы вы получите доступ к:

### ✅ Управление клиентами
- Карточки владельцев с полной информацией
- Карточки животных с историей болезней
- Поиск по всем параметрам
- Массовый импорт данных

### ✅ Расписание и приёмы
- Календарь с визуализацией
- Повторяющиеся приёмы
- Автоматические уведомления

### ✅ Вакцинация
- Учёт всех вакцинаций
- Генерация отчётов (PDF)
- Статистика по месяцам
- Напоминания о следующих прививках

### ✅ Назначения и лечение
- База лекарств и процедур
- Калькулятор стоимости
- История назначений

### ✅ Статистика
- Графики по вакцинациям
- Анализ по видам животных
- Финансовые отчёты
- Выбор периода для анализа

### ✅ ML Диагностика
- Автоматическая диагностика по симптомам
- База заболеваний животных
- Рекомендации по лечению

### ✅ Настройки
- Переключение тёмной/светлой темы
- Автоматическое резервное копирование
- Информация о программе

---

## ⚠️ Решение проблем

### Проблема: "python3: command not found"

**Причина:** Python 3 не установлен или не добавлен в PATH

**Решение:**
1. Установите Python 3.10+ с официального сайта: https://www.python.org/downloads/
2. Или из Microsoft Store: https://www.microsoft.com/store/productId/9PJPW5LDXLZ5
3. При установке отметьте "Add Python to PATH"

### Проблема: "No module named 'flask'"

**Причина:** Не установлены зависимости

**Решение:**
```bash
python3 -m pip install -r requirements.txt
```

### Проблема: Программа запускается, но в консоли ошибки кодировки

**Причина:** Проблемы с UTF-8 в Windows

**Решение:** Уже исправлено в коде! Если проблема осталась:
```bash
chcp 65001
python3 app.py
```

### Проблема: Порт 5000 уже занят

**Причина:** Другая программа использует порт 5000

**Решение 1:** Закройте программу, использующую порт

**Решение 2:** Измените порт в `app.py` (последняя строка):
```python
app.run(host='0.0.0.0', port=5001, debug=True)  # Используем порт 5001
```

### Проблема: PDF отчёты не генерируются

**Причина:** Не установлен wkhtmltopdf

**Решение:** См. [WKHTMLTOPDF.md](WKHTMLTOPDF.md)

**Примечание:** Программа работает и без wkhtmltopdf, просто PDF отчёты будут недоступны.

### Проблема: База данных повреждена

**Решение:**
```bash
reset_db.bat  # ВНИМАНИЕ: удалит все данные!
```

Перед сбросом рекомендуется сделать копию файла `instance/vet_clinic.db`

---

## 🔐 Безопасность

### Резервное копирование

Программа **автоматически** создаёт резервные копии базы данных:
- Частота: каждые 15 минут
- Место: папка `database_backups/`
- Хранятся последние 10 копий

### Ручное создание бэкапа

Откройте в браузере: http://localhost:5000/admin

---

## 🌐 Сеть

### Доступ с других устройств в локальной сети

1. Узнайте IP-адрес компьютера:
```bash
ipconfig
```

2. Найдите IPv4 адрес (например, 192.168.1.100)

3. На другом устройстве откройте: http://192.168.1.100:5000

**Примечание:** Убедитесь, что брандмауэр разрешает подключения к порту 5000.

---

## 📱 Поддерживаемые браузеры

Программа протестирована и работает в:
- ✅ Google Chrome
- ✅ Microsoft Edge
- ✅ Mozilla Firefox
- ✅ Safari
- ✅ Opera

---

## 🎓 Дополнительная информация

### Структура проекта

```
Aibolit/
├── app.py                 # Основной файл приложения
├── models.py              # Модели базы данных
├── forms.py               # Формы
├── templates/             # HTML шаблоны
├── static/                # CSS, JavaScript, изображения
├── instance/              # База данных
│   └── vet_clinic.db
├── database_backups/      # Резервные копии
├── docs/                  # Документация
└── ml/                    # ML модели
```

### Полезные файлы

- `requirements.txt` - список зависимостей
- `aibolit.bat` - запуск программы
- `reset_db.bat` - сброс базы данных
- `card.docx` - шаблон карточки животного

---

## 💡 Советы

1. **Регулярно делайте бэкапы** - автоматические копии хранятся только 10 штук
2. **Используйте темную тему** для комфортной работы вечером
3. **Изучите статистику** для планирования закупок
4. **Используйте ML диагностику** как вспомогательный инструмент

---

## 📞 Поддержка

- **GitHub:** https://github.com/Stuard256/Aibolit
- **Документация:** См. папку `docs/`

---

**Готово! Приятной работы! 🐾**

